en:
  time:
    formats:
      pretty: "%Y-%m-%d %H:%M:%S"
      long_pretty: "%B %d, %Y %H:%M:%S"
      date_only: "%Y-%m-%d"
      statistic: "%A, %B %d, %Y"
      time_only: "%H:%M"
      input: "%b %d %Y - %H:%M"
      range_start: "%A, %b %d, %Y %H:%M"
      range_end: "%H:%M"
  date:
    formats:
      date_only: "%Y-%m-%d"
      default: "%Y-%m-%d"
      short: "%b %d"
      long: "%B %d, %Y"
      time_only: "%H:%M"
    order:  
      - :month
      - :day
      - :year
  views:
    pagination:
      first: "&laquo; First"
      last: "Last &raquo;"
      previous: "&lsaquo; Prev"
      next: "Next &rsaquo;"
      truncate: "..."
  model:
    errors:
      custom:
        starts_at: "cannot start before now"
        ends_at: "cannot start before it ends"
