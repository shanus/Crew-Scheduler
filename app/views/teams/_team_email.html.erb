<% if @current_team.users.size > 1 %>
  <%= mail_to team_email(@current_team), encode: "hex", class: "btn btn-outline-primary btn-sm mb-2", title: "Send email to all team members" do %>
    <i class="bi bi-envelope"></i> Email Entire Crew
  <% end %>
<% end %>
